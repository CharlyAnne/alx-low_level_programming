#include <stdio.h>
/**
 * main - Entry point
 * Description: 'output base 10 numbers'
 * Return: Always 0 (Succes)
 */
int main(void)
{
int count = 0;
while (count < 10)
printf("%d", count++);
printf("\n");
return (0);
}
