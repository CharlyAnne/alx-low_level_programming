#include "main.h"

/**
 * _strlen - function to return the length of a string
 * @s: number to be tested
 *
 * Return: Always 0.
*/
int _strlen(char *s)
{
int i;
for (i = 0; s[i] != '\0'; i++)
return (i);
}
